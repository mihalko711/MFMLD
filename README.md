# MFMLD — Machine Learning for Machinery Fault Detection and Diagnosis

Проект посвящён разработке и исследованию моделей машинного обучения для выявления и диагностики неисправностей в механическом оборудовании (подшипники качения) на основе анализа сигналов вибрации.

## 📋 Содержание

- [Задача](#-задача)
- [Датасеты](#-датасеты)
- [Структура проекта](#-структура-проекта)
- [Модули](#-модули)
- [Ноутбуки](#-ноутбуки)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)

---

## 🎯 Задача

Основная цель проекта — **выявление аномалий и прогнозирование деградации подшипников** с помощью:

1. **Автоэнкодеров (AE)** — обучение реконструкции нормальных сигналов вибрации. Аномалии выявляются по величине ошибки реконструкции.
2. **Моделей прогнозирования (Forecasters)** — предсказание будущих значений сигнала на основе предыдущих. Увеличение ошибки прогноза может свидетельствовать о развитии дефекта.

### Подходы к обработке сигналов:

- **Временной сигнал (Raw)** — работа с сырыми данными вибрации
- **Спектральный анализ (FFT)** — преобразование сигнала в частотную область
- **Mel-спектрограммы** — логарифмические спектрограммы, адаптированные к восприятию человеком

---

## 📊 Датасеты

Проект использует следующие датасеты:

### NASA IMS Bearing Dataset
- **Описание**: Данные вибрации подшипников, собранные до момента отказа
- **Формат**: Текстовые файлы (.txt) с разделителем TAB
- **Структура**: 4 канала вибрации (bearing 1-4), 20480 точек на файл
- **Частота дискретизации**: 20 kHz

### CWRU Bearing Dataset
- **Описание**: Данные Case Western Reserve University с дефектами подшипников
- **Формат**: MATLAB файлы (.mat)
- **Типы дефектов**:
  - Normal (нормальное состояние)
  - Inner Race (дефект внутреннего кольца)
  - Outer Race (дефект внешнего кольца)
  - Ball (дефект шарика)
- **Серьёзность дефектов**: 7, 14, 21 mils

### MAFAULDA Dataset
- **Описание**: Синтетические данные с различными типами неисправностей
- **Типы неисправностей**:
  - Horizontal/Vertical misalignment (несоосность)
  - Imbalance (дисбаланс)

---

## 📁 Структура проекта

```
MFMLD/
├── conf/                    # Конфигурационные файлы (Hydra)
│   └── config.yaml
├── data/                    # Данные
│   └── 2nd_test/           # NASA IMS данные
├── models/                  # Сохранённые модели (.pt)
├── notebooks/               # Jupyter ноутбуки для исследований
├── src/                     # Исходный код
│   ├── autoencoders/       # Автоэнкодеры для выявления аномалий
│   ├── forecasters/        # Модели прогнозирования
│   ├── unet1d/             # 1D U-Net архитектуры
│   └── unet2d/             # 2D U-Net архитектуры
├── weights/                 # Веса обученных моделей
├── weights_raw/            # Сырые веса моделей
├── requirements.txt        # Зависимости Python
└── README.md               # Этот файл
```

---

## 🧩 Модули

### `src/autoencoders/`
Модуль для обучения автоэнкодеров на выявление аномалий.

**Файлы:**
- `datasets.py` — классы датасетов для NASA IMS:
  - `NASAIMSRawDataset` — сырые сигналы вибрации
  - `NASAIMSSpectrumDataset` — FFT спектры
  - `NASAIMSMelDataset` — Mel-спектрограммы
- `train_loop.py` — цикл обучения автоэнкодера (`ae_train_loop`)

### `src/forecasters/`
Модуль для обучения моделей прогнозирования.

**Файлы:**
- `datasets.py` — классы датасетов, возвращающие пары (input, target):
  - `NASAIMSRawDataset` — прогнозирование следующего окна сигнала
  - `NASAIMSSpectrumDataset` — прогнозирование спектров
  - `NASAIMSMelDataset` — прогнозирование Mel-спектрограмм
- `train_loop.py` — цикл обучения моделей прогнозирования (`forecast_train_loop`)

### `src/unet1d/`
Одномерная U-Net архитектура для обработки временных рядов.

**Файлы:**
- `model.py` — класс `UNet1D` с параметрами:
  - `entry_channels` — количество входных каналов
  - `n_channels` — базовое количество каналов
  - `ch_mults` — множители каналов для каждого разрешения
  - `is_attn` — использование attention блоков
  - `n_blocks` — количество residual блоков
  - `act` — функция активации
- `load_save.py` — функции для сохранения/загрузки чекпоинтов:
  - `save_model_checkpoint()`
  - `load_model_checkpoint()`

### `src/unet2d/`
Двумерная U-Net архитектура для обработки спектрограмм.

**Файлы:**
- `model.py` — класс `UNet` с параметрами:
  - `image_channels` — количество каналов изображения
  - `n_channels` — базовое количество каналов
  - `ch_mults` — множители каналов
  - `is_attn` — использование attention блоков
  - `n_blocks` — количество блоков
- `load_save.py` — функции для сохранения/загрузки чекпоинтов

---

## 📓 Ноутбуки

### `nasa_ims_EDA.ipynb`
Исследовательский анализ данных (EDA) датасета NASA IMS.
- Загрузка и визуализация сигналов
- Анализ статистических характеристик
- Выявление трендов деградации

### `u_net_prototype.ipynb`
Прототипирование U-Net архитектуры для 1D сигналов.
- Реализация архитектуры
- Тестирование на синтетических данных

### `ae_exp.ipynb`
Эксперименты с автоэнкодерами.
- Обучение различных архитектур AE
- Сравнение качества реконструкции
- Визуализация результатов

### `forecast_exp.ipynb`
Эксперименты с моделями прогнозирования.
- Обучение моделей на парах (input, target)
- Анализ ошибок прогноза
- Прогнозирование деградации

### `forecast_method.ipynb`
Методы прогнозирования для CWRU датасета.
- Загрузка .mat файлов
- Обработка сигналов
- Классификация типов дефектов

### `AE_comparison_MAFAULDA.ipynb`
Сравнение автоэнкодеров на датасете MAFAULDA.
- Benchmark различных архитектур:
  - MLP Autoencoder (Time/FFT)
  - Conv Autoencoder (Time/FFT)
  - ConvBiLSTM Autoencoder
- Метрики качества: ROC-AUC, PR-AUC, KS statistic
- Анализ для различных типов неисправностей

---

## ⚙️ Установка

### Через conda/micromamba

```bash
# Создание окружения
micromamba create -n mfml -f exports.yaml
# или
conda env create -n mfml -f exports.yaml

# Активация
micromamba activate mfml
# или
conda activate mfml
```

### Через pip

```bash
pip install -r requirements.txt
```

### Основные зависимости

- **Python**: 3.x
- **PyTorch**: 2.9.0
- **Polars**: 1.33.0 (быстрая обработка данных)
- **Hydra**: 1.3.2 (управление конфигурациями)
- **Scikit-learn**: 1.7.1
- **Matplotlib/Seaborn**: визуализация

---

## 🔧 Конфигурация

Конфигурационный файл `conf/config.yaml` содержит параметры обработки данных:

```yaml
data:
  window_size: 1024      # Размер окна в точках
  overlap: 768           # Перекрытие окон
  spectral: false        # Использовать спектры
  sample_rate: 20000     # Частота дискретизации (Hz)
  f_max: 5000           # Максимальная частота для Mel
  n_mels: 64            # Количество Mel-фильтров
  chunk_size: 8192      # Размер чанка для Mel
  chunk_overlap: 6144   # Перекрытие для Mel
```

---

## 📚 Использование

### Пример обучения автоэнкодера

```python
from src.autoencoders.train_loop import ae_train_loop
from src.autoencoders.datasets import NASAIMSRawDataset
from src.unet1d.model import UNet1D
from src.unet1d.load_save import save_model_checkpoint

# Создание датасета
train_ds = NASAIMSRawDataset(file_paths=train_files, window_size=1024)
test_ds = NASAIMSRawDataset(file_paths=test_files, window_size=1024)

# Создание модели
model = UNet1D(entry_channels=1, n_channels=16, ch_mults=(1, 2, 4))

# Обучение
ae_train_loop(
    model=model,
    train_ds=train_ds,
    test_ds=test_ds,
    optimizer=torch.optim.Adam(model.parameters(), lr=1e-3),
    epochs=50,
    device='cuda',
    batch_size=32
)
```

### Пример обучения модели прогнозирования

```python
from src.forecasters.train_loop import forecast_train_loop
from src.forecasters.datasets import NASAIMSRawDataset

# Датасет возвращает пары (input, target)
train_ds = NASAIMSRawDataset(file_paths=train_files, window_size=1024)
test_ds = NASAIMSRawDataset(file_paths=test_files, window_size=1024)

# Обучение
history = forecast_train_loop(
    model=model,
    train_ds=train_ds,
    test_ds=test_ds,
    optimizer=torch.optim.Adam(model.parameters(), lr=1e-3),
    epochs=50,
    device='cuda',
    batch_size=32
)
```

---

## 📝 Авторы

Проект разработан для исследования методов машинного обучения в задачах прогнозирования технического состояния оборудования (Predictive Maintenance).

---

## 📄 Лицензия

Этот проект является учебным/исследовательским. Использование датасетов регулируется их лицензиями.
